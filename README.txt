Structure

Admin
  |
Users

Functions to be implemented:

Admin: 
Auth
Users CRUD 

Users: 
Auth

CRUD grocery:
Add grocery(item, quantity) # by key
Add grocery(items) # by 2D list
Get all items of inventory # displaying all items of dict/hashmap by keys
Update grocery(item, quantity, item1, quantity1)
Delete grocery (item)

Generate recipe(items) # by list. call api functions url already hosted python flask?
Generate recipe(image) # by jpeg, jpg, png ... files using camera using yolo9 (Minh). call api already hosted
